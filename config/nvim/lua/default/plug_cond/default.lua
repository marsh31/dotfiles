-- default.lua
--
--

return {
    all = true,

    -- treesitter
    treesitter = true,

    -- which-key
    which_key_nvim = true,

    -- coding
    nvim_autopairs = true,

    -- filer ( fern / nvim-tree / oil )
    fern = false,
    nvimtree = false,
    oil = true,

    snictemplate = true,

    -- task runner
    overseer = true,

    pretty_fold = true,

    -- utils
    nvim_surround = true,
    comment = true,
    auto_session = true,

    -- orgmode
    orgmode = true,
}
